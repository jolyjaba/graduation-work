import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'graduation-work',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
